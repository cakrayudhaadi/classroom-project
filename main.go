package main

import "the-classroom/configs"

func main() {
	configs.Initiator()
	// connection.Initiator()
	// defer connection.SqlDBConnections.Close()
	// migration.Initiator(connection.SqlDBConnections)
	// routers.StartServer()
}
